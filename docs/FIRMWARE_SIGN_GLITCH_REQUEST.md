# 로드셀 펌웨어 수정 요청 — RQIW 응답 부호 비트 손상 (스캔 슬롯 순회형)

> 관련 이슈: CHAI-Student/CRK-IO-BOARD#1 (부호 왕복 플리커).
> 원시 캡처 데이터: issue #1 코멘트 첨부 `flicker.jsonl` (664프레임).

## 1. 요약

로드셀 무게 조회(`RQIW`) 응답의 **약 12%에서 1~2개 채널의 부호가 반전**되어
보고됩니다. 크기(절대값)는 완전히 보존되고 부호만 뒤집히며, 손상은 채널당
**정확히 1프레임** 지속되고, 손상되는 채널 위치가 **스캔 순서대로 한 칸씩
순회**합니다. 이 서명은 개별 셀·배선 문제가 아니라 **MCU가 디지털 셀
응답(부호 = X4 바이트 bit7)을 읽어 프레임을 조립하는 단계에서 주기적
이벤트(타이머 인터럽트 등)와 레이스가 발생**함을 가리킵니다. 크기와 부호를
동일 스냅샷에서 원자적으로 조합하도록 수정을 요청합니다.

## 2. 관측 데이터 (실기)

수집 조건: `GET /loadcells` 폴링 ~90ms 간격, 60초, 664프레임, 전 채널 적재
상태(24~962g). 원시 프레임 발췌:

```
frame 1: ch6만 음수   ["+00962","+00522","+00520","+00519","+00129","+00052","-00696","+00080","+00084","+00552"]
frame 2: ch7만 음수   ["+00962","+00522","+00520","+00519","+00129","+00052","+00696","-00080","+00084","+00552"]
frame 3: ch8·9만 음수 ["+00962","+00522","+00520","+00519","+00129","+00052","+00696","+00080","-00084","-00552"]
frame 4: ch0만 음수   ["-00962","+00522","+00520","+00519","+00129","+00052","+00696","+00080","+00084","+00552"]
```

정량 분석 결과:

| 항목 | 측정값 |
|---|---|
| 프레임당 음수(손상) 채널 수 | 0개: 101, 1개: 365, 2개: 181, 3개: 16 프레임 |
| 손상 위치 이동 | 관측 간 +1칸이 229회로 압도적 (+3은 폴링이 내부 스캔을 건너뛴 것) |
| 2채널 손상 프레임 | 181개 중 180개가 **인접 채널 쌍** |
| 손상값 크기 | 직전 정상값 대비 차이 **median 0.0g** (크기 완전 보존) |
| 손상 지속 | 782회 중 781회가 **정확히 1프레임** |
| 채널별 손상률 | 전 채널 11.3~12.0%로 균일 |

## 3. 추정 원인

사용 중인 디지털 셀(LABD-B3/K3) 프로토콜에서 무게값은
`[St 상태][X4: division 코드 + bit7 부호][X3 X2 X1: 크기]`의
**부호-크기(sign-magnitude) 분리 표현**입니다. 관측된 "크기 보존 + 부호만
반전 + 스캔 슬롯 순회"는 다음과 정합합니다:

- MCU가 셀들을 순차 폴링하며 응답을 파싱하는 도중, 고정 주기의 이벤트(틱
  인터럽트, 다른 통신 처리 등)가 개입하는 순간 **X4의 bit7만 잘못
  읽히거나 스테일 값으로 조합**됨.
- 이벤트 주기와 스캔 주기의 위상차 때문에 손상 슬롯이 프레임마다 한 칸씩
  이동 (인접 2채널 동시 손상은 이벤트가 슬롯 경계에 걸친 경우).

## 4. 요청 사항

1. **(필수) 크기·부호 원자적 조합**: 셀 응답의 X4(부호)와 X3X2X1(크기)을
   동일 수신 버퍼 스냅샷에서 읽어 ASCII 변환까지 인터럽트에 영향받지 않게
   처리해 주십시오. 검증 기준은 §5 참조.
2. **(권장) St 상태 바이트 전달**: 셀이 보내는 St의 Stable(bit1)·에러
   비트들이 현재 6문자 변환에서 소거됩니다. 프로토콜 확장 여지가 있다면
   안정 플래그 전달을 검토해 주십시오 (상위 판정 시스템의 안정 구간 검출을
   하드웨어 신호로 대체 가능).
3. **(참고) 분해능 정합**: 센서 스펙이 division 1g / **분해능 5g**이므로,
   장기적으로 펌웨어 또는 셀 설정(division 레지스터, d=5 지원)에서 5g 단위
   보고로 정합하는 방안도 논의를 요청합니다.

## 5. 재현·검증 방법

에지 장비의 진단 스크립트로 수정 전/후를 동일 기준으로 비교할 수 있습니다:

```bash
python3 loadcell_diag.py run -o capture.jsonl --duration 60
```

- 수정 전: 섹션 [1]에서 채널당 sign-flips ~150회/664프레임, duty 0.88.
- 수정 후 합격 기준: **전 채널 sign-flips = 0** (적재 정지 상태 60초).

## 6. 엣지 임시 조치 (참고)

펌웨어 수정 전까지 IO-BOARD 파이썬 서비스 `get_loadcells()` 관문에
글리치 보정(미디언-of-3 + 부호 연속성 가드)을 적용했습니다
(`src/services/io_board/sanitizer.py`, `IO_BOARD__SANITIZE__*` 설정).
실측 캡처 리플레이 기준 오염 샘플 채널당 79→3개(부팅 직후 0.3초분)로
복구됩니다. 펌웨어 수정 후에도 이 보정은 무해하게 동작하지만(no-op),
근본 수정은 펌웨어 단이 맞습니다.
